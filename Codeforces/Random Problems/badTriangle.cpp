#include <bits/stdc++.h>
#include <iostream>
using namespace std;
int main(){
    int n;
    cin >> n;
    for (int i = 0; i < n; i++){
        int l;
        cin >> l;
        vector<int> array;
        for(int k=0; k < l; k++){
            int m;
            cin >> m;
            array.push_back(m);
        }

        for(auto num: array){
            
        }
    }


    cout << "Done";
}